## Unreleased

- Accept only rules version 3 and assign every newborn cell the majority color
  of its exactly three live neighbors.
- Remove the obsolete `player` input from `evolve`.
- Accept only the canonical `op` field and operation names.

## 1.0.0

- Initial version.
