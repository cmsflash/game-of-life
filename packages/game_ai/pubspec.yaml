name: game_ai
description: Deterministic baseline agents and experiment runners for Life Duel.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  crypto: ^3.0.7
  game_engine:
    path: ../game_engine

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
