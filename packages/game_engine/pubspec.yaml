name: game_engine
description: Deterministic rules engine for the two-player Life Duel game.
version: 1.0.0

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
